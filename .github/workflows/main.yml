name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  # Cancels pending runs when a PR gets updated.
  group: ${{ github.head_ref || github.run_id }}-${{ github.actor }}
  cancel-in-progress: true

permissions:
  # Sets permission policy for `GITHUB_TOKEN`
  contents: read

jobs:
  tests:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest

    container:
      image: ghcr.io/jacobcrabill/alpine-zig:0.14.1

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Change if you need git info

      - name: Build x86 Linux
        run: zig build -Dllvm -Dtarget=x86_64-linux-musl

      - name: Test
        run: zig build -Dllvm -Dtarget=x86_64-linux-musl test

      - name: Build aarch64 Linux
        run: zig build -Dllvm -Dtarget=aarch64-linux

      - name: Build x86 MacOS
        run: zig build -Dllvm -Dtarget=x86_64-macos

      - name: Build aarch64 MacOS
        run: zig build -Dllvm -Dtarget=aarch64-macos

      - name: Build Windows
        run: zig build -Dllvm -Dtarget=x86_64-windows
